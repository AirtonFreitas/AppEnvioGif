import 'package:flutter/material.dart';

class TituloHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('Vamos enviar um GIF?',
    style: TextStyle(
      fontWeight: FontWeight.bold,
      fontSize: 25,
      color: Colors.blueAccent
    ),);
  }
}
